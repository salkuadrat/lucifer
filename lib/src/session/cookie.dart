/* class Cookie {
  late String _path;
  late int? _maxAge;
  late bool _httpOnly;

  Cookie({
    String path = '/',
    int? maxAge,
    bool httpOnly = true,
  }) {
    _path = path;
    _maxAge = maxAge;
    _httpOnly = httpOnly;
  }
} */
